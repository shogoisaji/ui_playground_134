name: ui_playground_134
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router:
  font_awesome_flutter:
  url_launcher:
  url_launcher_web:
  auto_size_text:
  rive:
  flutter_svg:
  provider:
  cached_network_image:
  video_player:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/gif/
    - assets/thumbnails/
    - assets/rive/
    - lib/pages/m_line_chart/data.json
